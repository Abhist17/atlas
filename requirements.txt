# Pinned to what Atlas is actually tested against. pandas-ta in particular
# tracks pandas/numpy closely, and an unpinned install has silently broken the
# indicator stack before.
#
# Python 3.12+

# --- Core data / compute -------------------------------------------------
pandas==3.0.3
numpy==2.2.6
pyarrow==24.0.0
pandas-ta==0.4.71b0
numba==0.61.2

# --- Market data ---------------------------------------------------------
yfinance==1.5.1
requests==2.34.2
tenacity==9.1.4

# --- Broker (Dhan) -------------------------------------------------------
dhanhq==2.2.0
websockets==16.1.1

# --- Scheduling ----------------------------------------------------------
APScheduler==3.11.3

# --- Web app -------------------------------------------------------------
fastapi==0.139.2
uvicorn==0.51.0
jinja2==3.1.6
itsdangerous==2.2.0
python-multipart==0.0.32

# --- Streamlit dashboard (dashboard/app.py) ------------------------------
streamlit==1.59.2
plotly==6.9.0

# --- Config --------------------------------------------------------------
pyyaml==6.0.3
python-dotenv==1.2.2
